1.	static: can't call a method from main directly unless it's static. 
	Can't call an inner class before you have an instance of the outer class, unless the inner class is static
2.	initialize array with generics: ArrayList<E>[] array = (ArrayList<E>[]) new ArrayList[size];